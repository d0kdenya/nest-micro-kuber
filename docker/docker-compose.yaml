version: "3.9"

services:
  postgres:
    image: postgres:latest
    container_name: micro-db-postgres
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - ./volumes/postgres:/var/lib/postgresql/data
    restart: unless-stopped